CREATE TABLE DEPOSIT
(
	ACTNO INT,
	CNAME VARCHAR(50),
	BNAME VARCHAR(50),
	AMOUNT DECIMAL(8,2),
	ADATE DATETIME
)
CREATE TABLE BRANCH
(
	BNAME VARCHAR(50),
	CITY VARCHAR(50)
)

CREATE TABLE CUSTOMERS
(
	CNAME VARCHAR(50),
	CITY VARCHAR(50)
)

CREATE TABLE BORROW
(
	LOANNO INT,
	CNAME VARCHAR(50),
	BNAME VARCHAR(50),
	AMOUNT DECIMAL(8,2)
)

--1
select * from DEPOSIT

--2
select * from BORROW

--3
select ACTNO,CNAME,AMOUNT
from DEPOSIT

--4
select 
	[dbo].[BORROW].[LOANNO],
	[dbo].[BORROW].[AMOUNT]
from [dbo].[BORROW]

--5
select *
from BORROW
where BNAME = 'ANDHERI'